%h1 Notes

%div#notes
  %h2#month
    = link_to "<", date: @date.prev_month
    = @date.strftime("%B %Y")
    = link_to ">", date: @date.next_month
  = calendar @date do |date|
    = date.day
    %br
    - unless @notes[date].nil?
      = link_to @notes[date].first.emotion, note_path(@notes[date])
